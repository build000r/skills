# Runtime-Safe Skill Bundle

Tracked reusable runtime-safe skills live here.

Rules:

- Keep this directory free of live instance IDs, client names, hostnames, and secrets.
- Only commit skills that are intended to be reused across multiple OpenClaw runtimes.
- Put private per-instance snapshots under `../instances/` and keep them ignored.
