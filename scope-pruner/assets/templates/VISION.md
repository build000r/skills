# Vision

<!-- Keep this under ~20 lines. If it cannot discriminate between "core" and
     "nice", it is too vague. -->

**User**: {the one person/role this exists for — not "everyone"}

**Job**: {the single outcome that user hires this project to deliver}

**Core loop**: {the repeated sequence that delivers the job, e.g.
"capture → process → review → export"}

**Winning means**: {the one metric or felt experience that says the job is
being done better than alternatives}

**Scope posture**: {EXPLORE | CONVERGE | MAINTAIN} (as of {date})

<!-- EXPLORE: pre-product-market-fit; a few time-boxed, disposable experiments
     are allowed, each with an expiry. CONVERGE: experiments must be absorbed
     into the core loop or retired. MAINTAIN: new surfaces default to out
     unless they replace an existing one. Reconsider only at milestones:
     first external users, paid launch, confirmed pivot, maintenance mode. -->

## Non-goals

<!-- At least 3. These are load-bearing: scope decisions cite them, and the
     scope ledger promotes recurring PARK clusters up into this list. -->

- {adjacent thing this project deliberately does not do}
- {audience this project deliberately does not serve}
- {capability that belongs in a different project/repo}

## Depth scoreboard

<!-- 3–5 measurable qualities of the core loop, derived from "winning means".
     Improvements to these numbers are the preferred achievement in this
     repo — report them as such. This gives agents a legible way to win at
     deepening instead of widening. -->

| Core-loop quality | Current | Target |
|-------------------|---------|--------|
| {e.g. capture-to-export time} | {value} | {value} |
| {e.g. crash-free session rate} | {value} | {value} |

## Scope guard

Before adding any feature, check `OUT_OF_SCOPE.md` and compare intent, not
names. New features must name which core-loop step they deepen (`deepens:`).
Features that add a new parallel surface require operator approval first.
